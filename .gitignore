# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Orphaned build output from repeated builds into the repo root (NOT the deploy)
/assets

# NOTE: /docs is intentionally tracked — it is the GitHub Pages source
# ("Deploy from branch: main /docs"). `npm run build` writes it.

# local-only single-file preview tooling (not part of the Pages build)
vite.config.preview.mts
/dist-preview
